<%= form_for(@question, as: :edit_question, url: question_path(@question)) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  	<br>
  <%= f.label :body %>
  <%= f.text_field :body %>
  	<br>
  <%= f.submit "Update Question" %>
<% end %>
