<%= form_for(@question, as: :new_question, url: questions_path(@question)) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  	<br>
  <%= f.label :body %>
  <%= f.text_field :body %>
  	<br>
  <%= f.submit "Add Question" %>
<% end %>
